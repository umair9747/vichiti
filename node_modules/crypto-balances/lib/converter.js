// Generated by CoffeeScript 1.10.0
(function() {
  var Big, conversion;

  Big = require('big.js');

  conversion = {
    "ETH": 1000000000000000000,
    "NXT": 100000000,
    "FCT": 100000000,
    "XEM": 1000000
  };

  module.exports = {
    toCoin: function(basicAmount, type) {
      var bigBasicAmount;
      if (typeof basicAmount === 'string') {
        basicAmount = Number(basicAmount);
      }
      bigBasicAmount = new Big(basicAmount);
      return Number(bigBasicAmount.div(conversion[type])).toString();
    },
    toSatoshi: function(coin, type) {
      var bigCoin;
      if (typeof coin === 'string') {
        coin = Number(coin);
      }
      bigCoin = new Big(coin);
      return Number(bigCoin.times(conversion[type])).toString();
    }
  };

}).call(this);
