// Generated by CoffeeScript 1.10.0
(function() {
  var _, bitshares, jsonRpc;

  _ = require("lodash");

  jsonRpc = require('jsonrpc-client');

  jsonRpc.setUrl('http://example.org/api');

  bitshares = function(addr) {
    return jsonRpc.call('saySomething', {
      to: 'Alice',
      message: 'Hi, Bob!'
    }).then(function(result) {
      return console.log(result);
    })["catch"](failure, function(err) {
      return console.error('Oops! Error code ' + err.code + ': ' + err.message);
    });
  };

  module.exports = bitshares;

}).call(this);
